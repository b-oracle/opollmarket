<!--
  Merge these into android/app/src/main/AndroidManifest.xml

  1) Add <uses-permission> entries inside <manifest>
  2) Add <service>, <activity>, <receiver> entries inside <application>
  3) Add intent-filter for the opoll:// scheme on your MainActivity
-->

<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
<!-- Lets the onboarding flow request battery-optimization exemption so FCM
     data-only call pushes are delivered while the device is dozing. -->
<uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
<uses-permission android:name="android.permission.VIBRATE" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
<uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
<uses-permission android:name="android.permission.TURN_SCREEN_ON" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<!-- Required by @capawesome-team/capacitor-android-foreground-service on Android 14+
     so the in-call mic stays alive when the WebView backgrounds. -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

<application ...>

    <!-- FCM data-only receiver that triggers the full-screen intent -->
    <service
        android:name=".CallMessagingService"
        android:exported="false">
        <intent-filter>
            <action android:name="com.google.firebase.MESSAGING_EVENT" />
        </intent-filter>
    </service>

    <!-- Lockscreen incoming-call UI.
         configChanges keeps the same Activity instance across rotation,
         keyboard show/hide, screen-size, ui-mode (day/night) and display-
         cutout changes — so the ringtone, pulse animation, and swipe state
         don't restart when the user tilts the device. -->
    <activity
        android:name=".IncomingCallActivity"
        android:exported="true"
        android:launchMode="singleTask"
        android:excludeFromRecents="true"
        android:showOnLockScreen="true"
        android:showWhenLocked="true"
        android:turnScreenOn="true"
        android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode|density"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />

    <!-- Accept/Decline broadcast -->
    <receiver
        android:name=".CallActionReceiver"
        android:exported="false" />

    <!-- Foreground service for the active in-call mic. The plugin auto-
         registers its own service class; this entry only needs to be
         present if you want to override the default declaration. The
         critical part is that the manifest declares
         foregroundServiceType="microphone" so Android 14+ keeps the mic
         hot even when the WebView is backgrounded.

         If you let the plugin auto-register, just ensure the two
         permissions above (FOREGROUND_SERVICE_MICROPHONE,
         FOREGROUND_SERVICE_MEDIA_PLAYBACK) are present. The plugin's
         service inherits the permissions declared at the manifest root.
    -->
    <!--
    <service
        android:name="io.capawesome.capacitorjs.plugins.foregroundservice.ForegroundService"
        android:foregroundServiceType="microphone|mediaPlayback"
        android:exported="false" />
    -->

    <!-- Deep-link on MainActivity so tapping Accept lands in the webview route -->
    <activity
        android:name=".MainActivity"
        ...>
        <intent-filter android:autoVerify="false">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="opoll" />
        </intent-filter>
    </activity>

</application>
